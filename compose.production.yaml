services:
  api:
    environment:
      DEBUG: "false"
      MAILER_ENABLED: "true"
      LOG_AUTH_CODES: "false"

  web_app:
    build: null
    image: nginx:1.27-alpine
    volumes:
      - ./web_app/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./web_app/dist:/usr/share/nginx/html:ro
