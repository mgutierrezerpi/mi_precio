[lint]
# FastAPI intentionally declares dependency injection through parameter defaults.
ignore = ["B008"]

[lint.isort]
known-first-party = ["config", "controllers", "infra", "lib", "tasks", "views"]
